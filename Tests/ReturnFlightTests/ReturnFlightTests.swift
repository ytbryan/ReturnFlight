    import XCTest
    @testable import ReturnFlight

    final class ReturnFlightTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(ReturnFlight().text, "Hello, World!")
        }
    }
